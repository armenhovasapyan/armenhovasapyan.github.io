{
  "name": "armenhovasapyan",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt generate",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:pdf": "node scripts/generate-cv-pdf.mjs --position=dotnet && node scripts/generate-cv-pdf.mjs --position=php && node scripts/generate-cv-pdf.mjs --position=nodejs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck"
  },
  "dependencies": {
    "nuxt": "^4.5.2",
    "vue": "^3.5.42",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.0.0",
    "pdf-lib": "^1.17.1",
    "tailwindcss": "^4.0.0",
    "typescript": "~5.9.0",
    "vue-tsc": "^2.1.0"
  }
}
